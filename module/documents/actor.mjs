/**
 * Erweiterte Actor-Klasse für Dragons and BallZ.
 * Hier landet später die eigentliche Spiellogik (z.B. Ki-Verbrauch,
 * Power-Level-Berechnungen, Transformationen ...).
 */
export default class DragonsBallZActor extends Actor {
  /** @override */
  prepareDerivedData() {
    super.prepareDerivedData();
  }
}
